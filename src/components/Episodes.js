import React from 'react';

import FormContainer from '../containers/FormContainer';

const Episodes = () => {
    return (
        <div>
            <FormContainer />
        </div>
    );
}

export default Episodes;
